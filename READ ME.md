# ことのは

時間帯・曜日に合わせて、元気が出るひとことを表示するWebアプリ。  
サーバー不要。HTMLファイルをブラウザで開くだけで動作する。

---

## 使い方

`kotoba.html` をブラウザで開く。それだけ。

開いた時刻と曜日を自動判定して、対応するメッセージを1件表示する。  
再読み込みするたびに別のことばが出る。

---

## 時間帯の区分

| 区分 | 曜日 | 時間 | トーン |
|------|------|------|--------|
| 平日の朝 | 月〜金 | 5〜11時 | 頑張る系 |
| 平日の昼 | 月〜金 | 11〜17時 | 中だるみ防止系 |
| 平日の夕方 | 月〜金 | 17〜21時 | もう一踏ん張り系 |
| 平日の夜 | 月〜金 | 21時〜翌5時 | よくやった系 |
| 週末の昼間 | 土・日 | 5〜18時 | 自分磨き系 |
| 週末の夜 | 土・日 | 18時〜翌5時 | 労わり系 |

---

## 画面の構成

- **ことば** — 中央に大きく表示。フェードインで出る。
- **出典**（任意）— 出典が設定されている場合のみ `— 著者名` と小さく表示。
- **♡ 好き** — 押すと殿堂入りに登録。もう一度押すと解除。
- **★ 殿堂**（右上）— 殿堂入りのことば一覧を表示。個別に削除可能。
- **時計**（右上）— 現在の日時を表示。

---

## 殿堂入り機能

- 「♡ 好き」を押したことばはブラウザの `localStorage` に保存される。
- 殿堂入りのことばは通常のローテーションに混入して表示される。
- ブラウザを変えるかキャッシュを削除すると消えるので注意。

---

## メッセージのカスタマイズ

`kotoba.html` をテキストエディタで開き、`const BASE = { ... }` の中を編集する。

### 出典なし（通常）

```js
"今日もあなたのペースで。"
```

### 出典あり（名言など）

```js
{text: "やってみなはれ。", source: "鳥井信治郎"}
```

文字列とオブジェクトは同じ配列内に混在できる。

### スロットのキー一覧

| キー | 対応する時間帯 |
|------|---------------|
| `wd_am` | 平日の朝 |
| `wd_no` | 平日の昼 |
| `wd_ev` | 平日の夕方 |
| `wd_ni` | 平日の夜 |
| `we_day` | 週末の昼間 |
| `we_eve` | 週末の夜 |

---

## メッセージ数

各スロット100語、合計600語。  
重複を避けてランダム表示するため、毎日使っても1スロットあたり約3ヶ月は同じことばが繰り返されない。

---

## 技術仕様

- 純粋なHTML/CSS/JavaScript。フレームワーク・ライブラリ不使用。
- 外部依存はGoogleフォント（Klee One・Zen Maru Gothic）のみ。オフライン時はシステムフォントにフォールバック。
- データ保存先：ブラウザの `localStorage`（殿堂入りのみ）。

### localStorageのキー

| キー | 内容 |
|------|------|
| `kotoba_hall` | 殿堂入りことば一覧 |
| `kotoba_msgs` | 月次生成メッセージ（将来拡張用・現在未使用） |
| `kotoba_used` | 表示済みインデックス（重複防止用） |
| `kotoba_schema` | localStorageスキーマバージョン（データ構造管理用） |

---

## バージョン管理

`kotoba.html` 内に2つのバージョン定数がある。

| 定数 | 役割 |
|------|------|
| `APP_VERSION` | コードの変更履歴を追うための表示用バージョン |
| `SCHEMA_VERSION` | localStorageのデータ構造バージョン |

### 言葉を追加・修正したとき
`APP_VERSION` のパッチ番号を上げる（例: `1.1.0` → `1.1.1`）。  
ファイル先頭のHTMLコメントに変更内容を1行追記する。

### localStorageの構造を変えたとき
`SCHEMA_VERSION` を上げる。  
次回起動時に `checkSchema()` が自動実行され、キャッシュ系データ（`kotoba_used` / `kotoba_msgs`）をリセットする。  
**殿堂データ（`kotoba_hall`）は保護されるため削除されない。**

---

## ファイル構成

```
kotoba.html   # アプリ本体（これ1ファイルで完結）
README.md     # このファイル
```
